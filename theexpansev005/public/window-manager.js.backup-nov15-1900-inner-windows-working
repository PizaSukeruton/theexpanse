(() => {
  const windowInners = [
    'left-window-inner',
    'top-right-window-inner',
    'bottom-right-window-inner'
  ];

  function init() {
    windowInners.forEach(id => {
      const el = document.getElementById(id);
      if (!el) {
        console.warn(`WindowManager: Element '${id}' not found.`);
        return;
      }
      el.classList.remove('wm-open');
      el.parentElement.addEventListener('click', () => toggle(id));
    });
    console.log('[WindowManager] ✅ Initialized');
  }

  function toggle(id) {
    const el = document.getElementById(id);
    if (!el) return;
    el.classList.toggle('wm-open');
  }

  window.WindowManager = { toggle };

  document.addEventListener('DOMContentLoaded', init);
})();
